name: example
description: "Flame Juice Box Example"

publish_to: 'none'# Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flame: ^1.18.0
  flame_juice_box:
    path: ../
  flutter:
    sdk: flutter
  nes_ui: ^0.20.0

dev_dependencies:
  flame_lint: ^1.1.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
